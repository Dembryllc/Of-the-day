---
date: 2026-06-30
project: OfTheDay.net
tags: [oftheday, deploy, session-log]
---

# OfTheDay.net — Session 2026-06-30

## Shipped (merged to `main`)
- **Slide export to PowerPoint / Google Slides** — `src/lib/exportSlide.js`
  (pptxgenjs, lazy-loaded ~368KB chunk). Buttons in `LessonSlideCreator.jsx`.
  Both export paths produce a `.pptx`; Google Slides opens it natively.
- **Landing page redesign** — showcases both tools + AI lesson slides;
  Two Tools section, interactive 4-theme slide mockup, 11-card feature grid.
  Merged cleanly with prior `main` polish.

## 🔴 Blocker to go-live (no code fix — config only)
Every deploy since 2026-06-26 fails at the CI **"Verify required secrets"** gate.
8 secrets missing from GitHub Actions (Stripe ×6, Mailgun ×2).
Firebase + Anthropic ARE present.

**Fix:** add the 8 via GitHub → Settings → Secrets and variables → Actions:
- VITE_STRIPE_MONTHLY_PRICE_ID = price_1Te35JB2eRKsbhTpqJrBmNRE
- VITE_STRIPE_ANNUAL_PRICE_ID  = price_1Te38IB2eRKsbhTp9GXJjxM0
- STRIPE_MONTHLY_PRICE_ID      = price_1Te35JB2eRKsbhTpqJrBmNRE
- STRIPE_ANNUAL_PRICE_ID       = price_1Te38IB2eRKsbhTp9GXJjxM0
- STRIPE_SECRET_KEY            = (Stripe dashboard → Developers → API keys)
- STRIPE_WEBHOOK_SECRET        = (Stripe dashboard → Developers → Webhooks)
- MAILGUN_API_KEY              = (Mailgun → Settings → API Keys)
- MAILGUN_DOMAIN               = mg.oftheday.net

Then re-run the deploy workflow → goes green.
**Never paste secret values into chat/terminal — GitHub secret store only.**

## Docs updated this session
- `CLAUDE.md` — Deploy gate, Slide Export, Landing Page, Pending Ops
- `HANDOFF.md` — current session brief prepended

## Next session
Check if the 8 secrets were added (GitHub Actions run status), then watch the
deploy clear the gate. Stripe still test mode — live-key swap is the step after.
